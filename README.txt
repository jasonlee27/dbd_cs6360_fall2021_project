For run the frontend application, please go to the directory /frontend and use the command npm
  npm i
  npm start
  npm run
  
For run the backend python, change the directory to /backend and use the command:
  ./run.sh 
